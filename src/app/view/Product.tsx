import { ProductList } from "../components/ProductList"

export const Product = () => {
    return (<>
        <section
            className="section-header bg-primary text-white"
            style={{ height: '90px', padding: 0 }}
        >
        </section>
        <ProductList />
    </>)
}